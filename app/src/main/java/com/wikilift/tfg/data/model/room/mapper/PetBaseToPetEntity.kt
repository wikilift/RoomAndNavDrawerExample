package com.wikilift.tfg.data.model.room.mapper

/*
fun PetBase.toPetEntity(): PetEntity = PetEntity(
    name = this.name,
    type = this.type,
    race = this.race,
    sexType = this.sexType,
    imgPath = this.imgPath,
    birthDate = this.birthDate,
    weight = this.weight,
    haveChip = this.haveChip,
    chipNumber = this.chipNumber
)

fun MutableList<PetBase>.toPetBaseList():MutableList<PetEntity>{
    val resultList= mutableListOf<PetEntity>()
    this.forEach{ resultList.add(it.toPetEntity())}
    return resultList
}
*/
